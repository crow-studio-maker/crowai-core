# Non-functional model package template

This directory demonstrates the CrowAI Contract v1 file layout and callback signatures. It is **not** an AI model and deliberately raises a clear error instead of fabricating an answer.

Copy it outside the Core, rename the mode/version directories as needed, implement and test a real reviewed pipeline, update the manifest, and validate it with `python -m tools.validate_model_package <path>`.
